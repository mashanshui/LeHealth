<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shenhesoft.lehealth">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/QMUI.Compat.NoActionBar.AppTheme">
        <activity android:name=".ui.activity.MainActivity" />
        <activity android:name=".ui.activity.LoginActivity">
        </activity>
        <activity android:name=".ui.activity.RegisterActivity" />
        <activity android:name=".ui.activity.ConditionActivity" />
        <activity android:name=".ui.activity.personal.PersonalHealthActivity" />
        <activity android:name=".ui.activity.personal.PersonalHelpActivity" />
        <activity android:name=".ui.activity.personal.PersonalAboutActivity" />
        <activity android:name=".ui.activity.personal.PersonalSetActivity" />
        <activity android:name=".ui.activity.DataAnalyzeActivity" />
        <activity android:name=".ui.activity.AnswerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>